<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HotDeskAndroid" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

    </style>

    <style name="TextBold" parent="Theme.MaterialComponents.DayNight">
        <!-- Alternatively, reference this font with the name "aspergit" -->
        <!-- Note that only our own TextView's will use the font attribute -->
        <item name="fontFamily">@font/sen</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/_12sdp</item>
        <item name="android:textColor">@color/figmaBlack</item>
    </style>

    <style name="TextRegular" parent="Theme.MaterialComponents.DayNight">
        <!-- Alternatively, reference this font with the name "aspergit" -->
        <!-- Note that only our own TextView's will use the font attribute -->
        <item name="fontFamily">@font/sen</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">@dimen/_11sdp</item>
    </style>
    <style name="TextSmall" parent="Theme.MaterialComponents.DayNight">
        <!-- Alternatively, reference this font with the name "aspergit" -->
        <!-- Note that only our own TextView's will use the font attribute -->
        <item name="fontFamily">@font/sen</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">@dimen/_9sdp</item>
        <item name="android:textColor">@color/figmaBlack</item>
    </style>
    <style name="TextExtrSmall" parent="Theme.MaterialComponents.DayNight">
        <!-- Alternatively, reference this font with the name "aspergit" -->
        <!-- Note that only our own TextView's will use the font attribute -->
        <item name="fontFamily">@font/sen</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">@dimen/_9sdp</item>
        <item name="android:textColor">@color/figmaBlack</item>
    </style>

</resources>