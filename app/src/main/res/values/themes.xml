<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HotDeskAndroid" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>
    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/top_round_corner_white</item>
        <item name="android:layout_marginTop">@dimen/_5sdp</item>
    </style>
    <style name="RoundedTabLayoutStyle" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@drawable/tabs_selector</item>
        <item name="tabIndicator">@null</item>
        <!--        <item name="android:background">@drawable/tab_layout_bg</item>-->
        <!--        <item name="tabSelectedTextColor">@color/colorAppTheme</item>-->
        <item name="tabTextColor">@color/white</item>
    </style>



    <style name="TextBoldButton" parent="Widget.MaterialComponents.Button.TextButton">
        <!-- Alternatively, reference this font with the name "aspergit" -->
        <!-- Note that only our own TextView's will use the font attribute -->
        <item name="fontFamily">@font/avenir_ltstd_black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/_12sdp</item>
        <item name="android:textColor">@color/figmaBlack</item>
    </style>
    <style name="chipText" parent="TextAppearance.MaterialComponents.Chip">
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">@font/avenir_ltstd_book</item>
    </style>
    <style name="TextBold" parent="Theme.MaterialComponents.DayNight">
        <!-- Alternatively, reference this font with the name "aspergit" -->
        <!-- Note that only our own TextView's will use the font attribute -->
        <item name="fontFamily">@font/avenir_ltstd_book</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/_12sdp</item>
        <item name="android:textColor">@color/figmaBlack</item>
    </style>

    <style name="TextRegular" parent="Theme.MaterialComponents.DayNight">
        <!-- Alternatively, reference this font with the name "aspergit" -->
        <!-- Note that only our own TextView's will use the font attribute -->
        <item name="fontFamily">@font/avenir_ltstd_book</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">@dimen/_11sdp</item>
    </style>
    <style name="TextSmall" parent="Theme.MaterialComponents.DayNight">
        <!-- Alternatively, reference this font with the name "aspergit" -->
        <!-- Note that only our own TextView's will use the font attribute -->
        <item name="fontFamily">@font/avenir_ltstd_book</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">@dimen/_9sdp</item>
        <item name="android:textColor">@color/figmaBlack</item>
    </style>
    <style name="TextExtrSmall" parent="Theme.MaterialComponents.DayNight">
        <!-- Alternatively, reference this font with the name "aspergit" -->
        <!-- Note that only our own TextView's will use the font attribute -->
        <item name="fontFamily">@font/avenir_ltstd_book</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">@dimen/_9sdp</item>
        <item name="android:textColor">@color/figmaBlack</item>
    </style>

    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/blue</item>
    </style>

    <style name="BlueCheck">
        <item name="colorAccent">@color/figmaBlue</item>
    </style>

</resources>